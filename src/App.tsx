import { BrowserRouter } from 'react-router-dom'

// internal imports
import Router from './Router'

export default function App() {
  return (
    <BrowserRouter>
      <Router />
    </BrowserRouter>
  )
}

