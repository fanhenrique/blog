# blog

To build this blog, you need to use [Pandoc](https://pandoc.org/), a file converter from one markdown format to another.

See how to install it on your operating system at [Installing Pandoc](https://pandoc.org/installing.html)

Each post is written in markdown format and converted to html.

The markdown metadata is converted to json.